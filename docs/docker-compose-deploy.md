# WindSentinel Docker Compose Deployment Guide

## Status
Deferred until after bare-metal deployment is complete.

## Rule
This document must only be finalized after the bare-metal deployment path is implemented and verified.

## Planned contents
- `docker-compose.yml` reference
- service definitions
- environment variables
- volume layout
- startup / shutdown / upgrade / troubleshooting steps
